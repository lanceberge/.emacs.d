# key: live
# name: live page
# --
defmodule `(+elixir--module-name-from-file)` do
  use AppWeb, :live_view

  @impl true
  def mount(_params, _session, socket) do
    $1

    socket =
      socket
      |> assign(:trading_accounts, accounts)

    {:ok, socket}
  end

  @impl true
  def render(assigns) do
    ~H"""
    $2
    """
  end

  @impl true
  def handle_event("$3", params, socket) do
    {:noreply, socket}
  end
end
